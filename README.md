# LA-Auction

**LA-Auction** — комплексная REST API платформа для управления автомобильными аукционами с интеграцией Copart и IAAI. Система обеспечивает полный цикл работы: от регистрации пользователей и загрузки лотов до проведения торгов в реальном времени и управления финансами.

---

## 📌 Описание

Проект представляет собой полнофункциональную платформу для автомобильных аукционов с поддержкой:
- Автоматического парсинга лотов с аукционных площадок (Copart, IAAI, Manheim)
- Системы торгов в реальном времени через WebSocket
- KYC верификации пользователей
- Финансовых операций (депозиты, транзакции, баланс)
- Многоязычности (en, ru, md, ua, kz, pl, ge, de)
- Real-time уведомлений через множество каналов

---

## 🛠️ Технологический стек

### Backend
- **FastAPI** — современный асинхронный веб-фреймворк
- **Python 3.12+** — основной язык разработки
- **Tortoise ORM** + **AsyncPG** — работа с БД
- **PostgreSQL** — основная СУБД
- **Pydantic** — валидация и сериализация данных

### Кеширование и очереди
- **Redis** — кеширование (aiocache) + брокер сообщений для Celery
- **Celery** — фоновые задачи (парсинг, создание лотов)
- **Kafka** — стриминг событий (audit logs, notifications)

### Хранение данных
- **MinIO (S3)** — хранение документов
- **Contabo S3** — хранение изображений лотов

### Безопасность
- **JWT** — аутентификация (access + refresh tokens)
- **bcrypt** — хеширование паролей
- **ClamAV** — антивирусная проверка загружаемых файлов
- **Fernet** — шифрование учетных данных аукционов
- **2FA** — двухфакторная аутентификация

### Интеграции
- **Playwright/Patchright** — автоматизация работы с аукционами
- **NHTSA API** — данные о безопасности автомобилей
- **Telegram/WhatsApp/Viber** — каналы уведомлений (planned)

### Инфраструктура
- **Docker** + **Docker Compose** — контейнеризация
- **Uvicorn** — ASGI сервер
- **CORS Middleware** — поддержка кросс-доменных запросов

---

## 🔄 User Flow

### Регистрация и верификация
1. Пользователь регистрируется (email + password)
2. Получает JWT токены (access 30 мин, refresh 7 дней)
3. Заполняет профиль (имя, страна, контакты, аватар)
4. Проходит KYC верификацию (загрузка документов)
5. После одобрения получает полный доступ

### Работа с депозитами
1. Пользователь создает запрос на депозит
2. Администратор проверяет и одобряет/отклоняет
3. При одобрении баланс пополняется, создается транзакция
4. Пользователь получает уведомление

### Поиск и отслеживание лотов
1. Поиск по фильтрам (марка, модель, год, цена, аукцион)
2. Добавление в watchlist (избранное)
3. Получение уведомлений об изменениях статуса/цены

### Участие в торгах
1. Подключение аккаунтов аукционов (Copart/IAAI)
2. Запуск бота для участия в live аукционах
3. Размещение ставок через API
4. Real-time обновления через WebSocket
5. Уведомления о результатах торгов

---

## 🚀 API Эндпоинты

Полная документация API доступна по адресу: `http://localhost:8000/docs` (Swagger UI)

### 🔐 Аутентификация и пользователи (`/user`)

| Метод | Эндпоинт | Описание | Auth |
|-------|----------|----------|------|
| POST | `/user/register` | Регистрация нового пользователя | - |
| POST | `/user/token` | Вход (получение JWT токенов) | - |
| POST | `/user/token/refresh` | Обновление access токена | - |
| POST | `/user/logout` | Выход (инвалидация refresh токена) | JWT |
| GET | `/user/me` | Получить информацию о текущем пользователе | JWT |
| GET | `/user/{user_id}/verification-status` | Статус KYC верификации | JWT |
| GET | `/user/admin` | Доступ к админ-панели | JWT + Admin |

**Логика:** Регистрация → bcrypt hash → автоназначение роли `basic_trader` → JWT токены (access 30 мин, refresh 7 дней)

---

### 👤 Профиль пользователя (`/profile`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/profile/me` | Получить свой профиль |
| PUT | `/profile/me` | Обновить профиль (имя, страна, контакты) |
| POST | `/profile/me/avatar` | Загрузить аватар (max 5MB) |
| DELETE | `/profile/me/avatar` | Удалить аватар |
| GET | `/profile/{user_id}` | Получить профиль пользователя (admin) |

**Поля профиля:** first_name, last_name, country, phone, tg_phone, tg_username, whatsapp_phone, viber_phone, avatar_url, balance

---

### 🚗 Управление лотами (`/lot`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/lot/refine` | Расширенная фильтрация лотов (кешировано, 30 мин) |
| POST | `/lot/create` | Создать новый лот (через Celery) |
| POST | `/lot/lots/batch` | Массовое создание лотов |
| GET | `/lot/id/{id}` | Получить лот по внутреннему ID |
| GET | `/lot/lot_id/{lot_id}` | Получить лот по auction lot_id |
| GET | `/lot/similar_lots/{id}` | Похожие лоты |
| GET | `/lot/lots_by_price` | Лоты в диапазоне цен |
| GET | `/lot/popular_brands` | Популярные марки |
| GET | `/lot/search_car` | Поиск по VIN/lot_id/модели |
| GET | `/lot/history/{vin}` | История автомобиля по VIN |
| GET | `/lot/special_filtered` | Спец. фильтры (buynow, low_odometer) |

**Архитектура:** 7 шардов (Lot1-Lot7) с round-robin распределением. Каждый лот имеет уникальный ID с префиксом. Кеширование в Redis (TTL 30 мин).

**Специальные таблицы:** HistoricalLot, LotWithoutAuctionDate, LotWithoutImage, LotOtherVehicle

---

### 🔗 Аккаунты аукционов (`/user_auction`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/user_auction/` | Создать аккаунт аукциона (Copart/IAAI) |
| PUT | `/user_auction/{account_id}` | Обновить учетные данные |
| DELETE | `/user_auction/{account_id}` | Удалить аккаунт |

**Безопасность:** Учетные данные шифруются Fernet (симметричное шифрование)

---

### ⭐ Watchlist (`/watchlist`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/watchlist/lots/{lot_id}/watch` | Добавить в избранное (max 100) |
| DELETE | `/watchlist/lots/{lot_id}/watch` | Удалить из избранного |
| GET | `/watchlist/lots/watchlist` | Получить список избранного |

---

### 💰 Депозиты (`/deposits`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/deposits/` | Создать запрос на депозит |
| GET | `/deposits/` | Получить свои депозиты (пагинация) |
| GET | `/deposits/{deposit_id}` | Получить конкретный депозит |
| PUT | `/deposits/{deposit_id}/approve` | Одобрить депозит (admin) |
| PUT | `/deposits/{deposit_id}/reject` | Отклонить депозит (admin) |
| GET | `/deposits/admin/all` | Все депозиты (admin) |

**Процесс:**
1. Создание → status: pending
2. Admin approve → user.balance += amount → Transaction record → Notification
3. Admin reject → status: failed → Notification with reason

**Типы оплаты:** bank_transfer, crypto, card, cash

---

### 📊 Транзакции (`/transactions`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/transactions/` | История транзакций (пагинация) |
| GET | `/transactions/balance` | Текущий баланс |

**Типы:** deposit, withdrawal, bid_hold, bid_release, bid_deduction, refund, fee

---

### 🔔 Уведомления (`/notifications`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/notifications/` | Получить уведомления |
| GET | `/notifications/unread-count` | Количество непрочитанных |
| PUT | `/notifications/{id}/read` | Отметить как прочитанное |
| PUT | `/notifications/read-all` | Отметить все как прочитанные |
| DELETE | `/notifications/{id}` | Удалить уведомление |
| GET | `/notifications/preferences` | Настройки каналов |
| PUT | `/notifications/preferences` | Обновить настройки |

**Типы:** deposit_received, deposit_failed, bid_placed, bid_outbid, auction_won, auction_lost, watchlist_update, kyc_approved, kyc_rejected

**Каналы:** In-app, Email, SMS, Telegram, WhatsApp, Push (опционально)

---

### 🔒 Двухфакторная аутентификация (`/2fa`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/2fa/enable` | Включить 2FA (TOTP) |
| POST | `/2fa/verify` | Проверить 2FA код |
| POST | `/2fa/disable` | Отключить 2FA |
| GET | `/2fa/status` | Статус 2FA |

---

### 📄 KYC (`/kyc`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/kyc/documents/upload` | Загрузить документ (passport, ID, selfie) |
| POST | `/kyc/submit` | Отправить заявку на KYC |

**Валидация документов:**
- Формат: JPEG, PNG, PDF (max 5MB)
- Разрешение: min 1280x720px, sharpness ≥ 95%
- Безопасность: ClamAV антивирус, magic bytes check, EXIF validation

---

### 🏪 Интеграция Copart (`/copart`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/copart/status` | Статус бота |
| POST | `/copart/start` | Запустить бота |
| POST | `/copart/stop` | Остановить бота |
| POST | `/copart/join` | Присоединиться к live аукциону |
| POST | `/copart/bid` | Разместить ставку |
| POST | `/copart/lot-details` | Получить детали лота |
| GET | `/copart/health` | Health check |

**Автоматизация:** Playwright, headless/headful режимы, сохранение сессий (SQLite), real-time участие в аукционах

---

### 🏪 Интеграция IAAI (`/iaai`)

Аналогичные Copart эндпоинты с автоматизацией через Playwright

---

### 🛠️ Администрирование (`/admin`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/admin/get_catalog` | Каталог лотов (пагинация) |
| GET/PUT | `/admin/translations` | Управление переводами (8 языков) |
| GET | `/admin/count/lots` | Количество активных лотов |
| GET | `/admin/count/lots_historical` | Исторические лоты |
| GET | `/admin/stats/vehicle_types` | Статистика по типам ТС |
| GET | `/admin/stats/auctions` | Статистика по аукционам |
| GET | `/admin/stats/monthly_trends` | Месячные тренды |
| CRUD | `/admin/reference/*` | Управление справочниками (Status, VehicleType, Make, Color) |
| GET | `/admin/healthcheck/s3` | Проверка S3 соединения |

**Система переводов:** en, ru, md, ua, kz, pl, ge, de. Fallback на английский.

---

### 📡 WebSocket (`/ws`)

**Подключение:** `ws://localhost:8000/ws?token={jwt}&lot_id={id}`

**События:**
- `subscribe` / `unsubscribe` — управление подписками
- `bid_update` — обновление ставки
- `status_update` — изменение статуса
- `auction_start` / `auction_end` — события аукциона
- `notification` — персональное уведомление

---

### 🔧 Дополнительные эндпоинты

| Префикс | Описание |
|---------|----------|
| `/additional/*` | Утилиты и вспомогательные функции |
| `/lead/*` | Управление лидами |
| `/nhts/*` | Интеграция с NHTSA API |
| `/task/*` | Статус фоновых задач Celery |
| `/calculator/*` | Калькулятор стоимости |
| `/translation/*` | Сервис переводов |
| `/audit/*` | Audit logs (журнал действий) |
| `/debug/*` | Debug утилиты (только dev) |

---

### 🏥 Health Check

**GET** `/health` — проверка работоспособности API (публичный доступ)

---

## 🗄️ Архитектура базы данных

### Шардирование лотов
- **7 шардов:** Lot1, Lot2, ..., Lot7
- **Round-robin распределение** через IDCounter
- **Prefix-based ID:** каждая таблица имеет уникальный префикс (10-17)
- **HistoricalLot:** архив завершенных аукционов

### Основные модели

**User:** id, email, password_hash, is_active, kyc_access, balance, profile fields, 2FA, roles

**Lot:** id, lot_id, vin, year, make, model, odometer, price, bid, auction_date, images, status, vehicle_type, damages, risk_index

**Deposit:** id, user, amount, currency, status, payment_method, transaction_id, payment_reference

**Transaction:** id, user, type, amount, balance_before, balance_after, status, deposit

**Notification:** id, user, type, title, message, is_read

### Связи
```
User → Deposits → Transactions
User → Notifications
User → Watchlist → Lot
Lot → VehicleType → Make → Model → Series
```

---

## ⚙️ Бизнес-логика

### Регистрация
1. POST /user/register → bcrypt hash → User → автоназначение роли "basic_trader" → JWT токены

### Депозит (Atomic transaction)
```python
async with in_transaction():
    balance_before = user.balance
    user.balance += amount
    await user.save()
    await Transaction.create(
        type=deposit, amount=amount,
        balance_before=balance_before,
        balance_after=user.balance
    )
    await Notification.create(...)
```

### Парсинг лотов
1. Парсер (Copart/IAAI) → JSON
2. POST /lot/lots/batch → Celery task
3. Для каждого лота:
   - Создание Vehicle hierarchy (VehicleType → Make → Model → Series)
   - Выбор шарда (round-robin)
   - Генерация ID с префиксом
   - Создание Lot записи
   - Загрузка изображений в S3
4. Инвалидация кеша

### Real-time обновления
1. WebSocket подключение: /ws?token=JWT&lot_id=123
2. Событие (bid_update) → ConnectionManager.broadcast_to_lot(lot_id)
3. Отправка всем подписчикам

---

## 🔐 Безопасность

- **JWT:** Access (30 мин) + Refresh (7 дней) токены
- **Хеширование:** bcrypt с salt
- **2FA:** TOTP (Google Authenticator)
- **Шифрование:** Fernet для учетных данных аукционов
- **Антивирус:** ClamAV для всех загружаемых файлов
- **Валидация:** Magic bytes, размер, формат, качество изображений
- **RBAC:** Role-Based Access Control (admin, basic_trader)
- **Audit Trail:** Kafka (audit_logs topic)

---

## 🚀 Установка и запуск

### Локальная разработка

```bash
git clone https://github.com/sashabodiul/la-auction
cd la-auction
cp .env.example .env
# Отредактируйте .env с вашими настройками
```

### Docker Compose

```bash
docker compose -f docker/docker-compose.yml up --build -d
```

Или с явным указанием .env:

```bash
docker compose --env-file .env -f docker/docker-compose.yml up --build
```

### Миграции БД

```bash
aerich migrate
aerich upgrade
```

### Celery Worker

```bash
celery -A app.tasks.celery_app worker --loglevel=info -c 4
```

---

## 🔧 Обслуживание

### Очистка дисковых логов

```bash
sudo truncate -s 0 /var/log/messages
sudo truncate -s 0 /var/log/kern.log
sudo truncate -s 0 /var/log/syslog
```

### Бэкапы БД

```bash
pg_dump -U user la_auction > backup_$(date +%Y%m%d).sql
```

---

## 📈 Производительность

- **Async/await:** полностью асинхронный код
- **Connection pooling:** PostgreSQL, Redis
- **Кеширование:** Redis (TTL 30 мин)
- **Шардирование:** 7 шардов для лотов
- **Индексы БД:** на часто запрашиваемых полях
- **Celery:** фоновые задачи

---

## 🐛 Известные проблемы и TODO

### В разработке
- [ ] Завершить bidding logic (эндпоинты есть, логика частичная)
- [ ] Внешние уведомления (Email, SMS, Telegram stubs)
- [ ] KYC approval workflow (частично)
- [ ] Avatar upload to S3 (placeholder)
- [ ] Rate limiting реализация
- [ ] Session management
- [ ] Comprehensive test coverage
- [ ] API versioning (v1, v2)

---

**Версия:** 1.0.0
**Последнее обновление:** 2025-11-25